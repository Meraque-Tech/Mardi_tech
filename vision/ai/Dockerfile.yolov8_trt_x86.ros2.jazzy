# meraquetech/race_nav:yolov8-trt-x86
# x86_64 — CUDA 12.x, TensorRT 10.x (RTX 5060 Blackwell compatible)
# Built on top of Dockerfile.yolov8_trt_x86.ros2.jazzy.dep, which provides the
# CUDA/TensorRT toolchain, ROS 2 Jazzy, and the web server's pip deps.
FROM meraquetech/race_nav:yolov8-trt-x86.dep

LABEL maintainer="meraquetech" \
      image="meraquetech/race_nav:yolov8-trt-x86"

SHELL ["/bin/bash", "-c"]

# -----------------------------
# Build yolov8_trt_bed_detect_orin
# -----------------------------
WORKDIR /ros2_ws/src
COPY yolov8_trt_bed_detect_orin/ yolov8_trt_bed_detect_orin/

WORKDIR /ros2_ws
RUN source /opt/ros/${ROS_DISTRO}/setup.bash && \
    colcon build --packages-select yolov8_trt_bed_detect_orin \
    --cmake-args -DCMAKE_BUILD_TYPE=Release

# Usage:
#   docker run ... ros2 launch yolov8_trt_bed_detect_orin bed_detect.launch.py
CMD ["/bin/bash", "-c", \
    "source /opt/ros/${ROS_DISTRO}/setup.bash && \
     source /ros2_ws/install/setup.bash && \
     ros2 launch yolov8_trt_bed_detect_orin bed_detect.launch.py"]
