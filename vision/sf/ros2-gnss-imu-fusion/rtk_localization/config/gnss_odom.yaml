rtk_localization:
  ros__parameters:
    fix_topic: /receiver/fix
    rtk_status_topic: /gnss/rtk_status
    odom_topic: /gnss/odom
    path_topic: /gnss/path
    is_forward_topic: /gnss/is_forward
    is_backward_topic: /gnss/is_backward

    map_frame: map
    base_frame: base_link

    require_rtk: false
    min_heading_distance: 3.0 # in meters
    stationary_timeout_s: 5.0 # in seconds
    direction_stale_timeout_s: 3.0 # in seconds
    reversal_angle_deg: 150.0 # in degrees
    direction_consistency_deg: 30.0 # in degrees
    heading_variance: 0.05
    fallback_horizontal_variance: 1.0
    fallback_vertical_variance: 4.0

    two_d_mode: true
    publish_tf: true
    path_max_poses: 5000
